# ==============================================================================
# Production Frontend Environment Variables Template (.env.frontend.prod.example)
# Copy this file to `.env.frontend.prod` and configure actual production values.
# ==============================================================================

# Next.js API client routing (Must match external URL routed by Nginx)
NEXT_PUBLIC_API_URL=https://tenderintel.yourdomain.com/api/v1
